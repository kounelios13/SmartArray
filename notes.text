check for bugs
self.getUniqueItems
self.removeDuplicates
self.sort() =>remove shoortcute for pre built function.The user has to provide its own sorting funtion
self.replaceItem()=> do not use updateProps() just use something like I used in swap